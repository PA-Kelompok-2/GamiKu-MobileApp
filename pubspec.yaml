name: application_gamiku
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  flutter_dotenv: ^5.0.2
  supabase_flutter: ^2.5.6
  get: ^4.6.5
  intl: ^0.19.0
  image_picker: ^1.0.7
  qr_flutter: ^4.1.0
  path: ^1.8.3
  confetti: ^0.7.0
  mobile_scanner: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/logo.png
    - assets/owner.png
    - assets/qris.png
